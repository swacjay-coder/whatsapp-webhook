name: Apply V60 Direct

on:
  workflow_dispatch:
  push:
    paths:
      - ".apply-v60-direct-trigger.txt"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply V60.2.7
        run: |
          set -e
          python3 v6027_fix_services_normalized_matching.py
          node --check server.js
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add server.js v6027_fix_services_normalized_matching.py
          git commit -m "Apply V60.2.7 fix services normalized matching" || echo "No changes"
          git push
