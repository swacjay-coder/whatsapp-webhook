name: Apply V60 Direct

on:
  workflow_dispatch:
  push:
    paths:
      - ".apply-v60-direct-trigger.txt"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply V60.2.6
        run: |
          set -e
          python3 v6026_fix_services_button_id_and_team_note.py
          node --check server.js
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add server.js v6026_fix_services_button_id_and_team_note.py
          git commit -m "Apply V60.2.6 fix services button id and team pause note" || echo "No changes"
          git push
