name: Apply V60 Direct

on:
  workflow_dispatch:
  push:
    paths:
      - ".apply-v60-direct-trigger.txt"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply V60.2.8.1
        run: |
          set -e
          python3 v60281_fix_how_button_title_only.py
          node --check server.js
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add server.js v60281_fix_how_button_title_only.py
          git commit -m "Apply V60.2.8.1 fix How button title only" || echo "No changes"
          git push
