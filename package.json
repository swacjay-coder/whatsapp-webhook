{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "WhatsApp webhook server",
  "main": "server.js",
  "scripts": {
    "start": "grep -q v31-5-8-59 server.js || git apply v59.patch && node --check server.js && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
