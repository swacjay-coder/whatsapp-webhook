{
  "name": "whatsapp-webhook",
  "version": "1.0.0",
  "description": "WhatsApp webhook server",
  "main": "server.js",
  "scripts": {
    "start": "node ui-polish-right-panel.js && node ui-favicon-only.js && node ui-clear-chat-local.js && node --check server.js && node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "node-fetch": "^2.6.7"
  }
}
