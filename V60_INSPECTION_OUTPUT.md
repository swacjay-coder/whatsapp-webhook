# V60 Inspection Output

Generated by GitHub Actions.

## BOT VERSION
69:const BOT_VERSION = "iconic-team-inbox-v31-5-8-59-direct-booking-intent-abu-dhabi-consultation-and-bot-pause";
3307:    version: BOT_VERSION,
3711:      botVersion: BOT_VERSION,
16622:    version: BOT_VERSION,

## FLOW BOOKING DATA
1999:function getWhatsAppFlowBookingData(message, lineConfig = {}) {
}

function normalizeFlowServiceInterest(value) {
  const cleanValue = compactText(value);

  if (cleanValue.includes("service_appointment") || cleanValue.includes("service appointment") || cleanValue.includes("سيرفس")) {
    return "Service Appointment";
  }

  if (cleanValue.includes("team") || cleanValue.includes("فريق")) {
    return "Talk to Team";
  }

  if (cleanValue.includes("hair_replacement") || cleanValue.includes("hair replacement") || cleanValue.includes("consultation")) {
    return "Hair Replacement Consultation";
  }

  return normalizeFlowChoiceText(value) || "Hair Replacement Consultation";
}

function getWhatsAppFlowBookingData(message, lineConfig = {}) {
  const payload = parseWhatsAppFlowResponse(message);

  if (!payload) {
    return null;
  }

  const branchValue = findFlowResponseValue(payload, ["branch", "فرع"]);
  const dayValue = findFlowResponseValue(payload, ["preferred_day", "day", "اليوم"]);
  const timeValue = findFlowResponseValue(payload, ["preferred_time", "time", "وقت"]);
  const serviceValue = findFlowResponseValue(payload, ["service_interest", "request_type", "نوع"]);
  const serviceTypeValue = findFlowResponseValue(payload, ["service_type", "service", "service appointment", "سيرفس"]);
  const consultationTypeValue = findFlowResponseValue(payload, ["consultation_type", "consultation", "consult type", "استشارة"]);
  const teamMemberValue = findFlowResponseValue(payload, ["team_member", "team member", "staff", "employee", "موظف", "فريق"]);
  const notesValue = findFlowResponseValue(payload, ["notes", "note", "preferred_exact_time", "ملاحظ"]);

  const branch = normalizeFlowBranch(branchValue, lineConfig.branch || "Dubai");
  const normalizedServiceType = normalizeFlowChoiceText(serviceTypeValue);
  const normalizedTeamMember = normalizeFlowChoiceText(teamMemberValue);
  const isServiceAppointment = Boolean(normalizedServiceType || normalizedTeamMember);

  const normalizedConsultationType = normalizeFlowChoiceText(consultationTypeValue);

  return {
    rawPayload: payload,
    branch,
    preferredDay: normalizeFlowDay(dayValue),
    preferredTime: normalizeFlowTime(timeValue),
    serviceInterest: isServiceAppointment ? "Service Appointment" : (normalizedConsultationType || normalizeFlowServiceInterest(serviceValue)),
    serviceType: normalizedServiceType,
    consultationType: normalizedConsultationType,
    teamMember: normalizedTeamMember,
    notes: notesValue || ""
  };
}

function buildWhatsAppFlowBookingRequestMessage(flowData = {}) {
  return [
    "Source: WhatsApp Flow",
    `Branch: ${flowData.branch || ""}`,
    `Preferred day: ${flowData.preferredDay || ""}`,
    `Preferred time: ${flowData.preferredTime || ""}`,
    `Service interest: ${flowData.serviceInterest || ""}`,
    flowData.serviceType ? `Service type: ${flowData.serviceType}` : "Service type: ",
    flowData.consultationType ? `Consultation type: ${flowData.consultationType}` : "Consultation type: ",
    flowData.teamMember ? `Team member: ${flowData.teamMember}` : "Team member: ",
    flowData.notes ? `Notes: ${flowData.notes}` : "Notes: ",
    "Flyksoft Status: Not added",
    "Customer selection: WhatsApp Flow submit"
  ].join("\n");
}

function buildWhatsAppFlowConfirmationBody(flowData = {}) {
  const branchAr = getFastBookingBranchArabic(flowData.branch || "Dubai");

  return [
    `${BUSINESS_NAME_SPACED} ✨`,
    "",
    "شكراً لك.",
    "تم استلام طلب الحجز.",
    "سيقوم فريقنا بمراجعة التوفر وتأكيد الموعد النهائي قريباً.",
    "",
    `الفرع: ${branchAr}`,
    `الوقت المفضل: ${flowData.preferredTime || "Flexible / Any available time"}`,
    "",
    "------------------------------",
    "",
    `${BUSINESS_NAME_SPACED} ✨`,
    "",
    "Your booking request has been received.",
    "Our team will check availability and confirm the exact appointment shortly.",
    "",
    `Branch: ${flowData.branch || "Dubai"}`,
    `Preferred time: ${flowData.preferredTime || "Flexible / Any available time"}`
  ].join(String.fromCharCode(10));
}


async function handleWhatsAppFlowBookingSubmit({
  from,
  message,
  incomingPhoneNumberId,
  lineConfig,
  profileName
}) {
  if (!isWhatsAppFlowReply(message)) {
    return false;
  }

  const flowData = getWhatsAppFlowBookingData(message, lineConfig);

  if (!flowData) {
    return false;
  }

  const selectedBranch = flowData.branch || lineConfig.branch || "Dubai";
  const requestMessage = buildWhatsAppFlowBookingRequestMessage(flowData);

  addInboxMessage(
    from,
    "customer",
    requestMessage,
    "Booking Request",
    incomingPhoneNumberId,
    {
      customerName: profileName,
      messageType: "WhatsApp Flow Submit"
    }
  );

  setConversationStatus(from, "Booking Request");
  await saveConversationStateToGoogleSheetFromServer({
    phone: from,
    phoneNumberId: incomingPhoneNumberId,
    branch: selectedBranch,
    status: "Booking Request",
    assignee: getBranchTeamAssignee(selectedBranch),
    tags: ["Booking", "WhatsApp Flow", "Need Confirmation"],
    updatedBy: "WhatsApp Flow"
  });


## BOOKING REQUEST MESSAGE
2035:function buildWhatsAppFlowBookingRequestMessage(flowData = {}) {
  const branch = normalizeFlowBranch(branchValue, lineConfig.branch || "Dubai");
  const normalizedServiceType = normalizeFlowChoiceText(serviceTypeValue);
  const normalizedTeamMember = normalizeFlowChoiceText(teamMemberValue);
  const isServiceAppointment = Boolean(normalizedServiceType || normalizedTeamMember);

  const normalizedConsultationType = normalizeFlowChoiceText(consultationTypeValue);

  return {
    rawPayload: payload,
    branch,
    preferredDay: normalizeFlowDay(dayValue),
    preferredTime: normalizeFlowTime(timeValue),
    serviceInterest: isServiceAppointment ? "Service Appointment" : (normalizedConsultationType || normalizeFlowServiceInterest(serviceValue)),
    serviceType: normalizedServiceType,
    consultationType: normalizedConsultationType,
    teamMember: normalizedTeamMember,
    notes: notesValue || ""
  };
}

function buildWhatsAppFlowBookingRequestMessage(flowData = {}) {
  return [
    "Source: WhatsApp Flow",
    `Branch: ${flowData.branch || ""}`,
    `Preferred day: ${flowData.preferredDay || ""}`,
    `Preferred time: ${flowData.preferredTime || ""}`,
    `Service interest: ${flowData.serviceInterest || ""}`,
    flowData.serviceType ? `Service type: ${flowData.serviceType}` : "Service type: ",
    flowData.consultationType ? `Consultation type: ${flowData.consultationType}` : "Consultation type: ",
    flowData.teamMember ? `Team member: ${flowData.teamMember}` : "Team member: ",
    flowData.notes ? `Notes: ${flowData.notes}` : "Notes: ",
    "Flyksoft Status: Not added",
    "Customer selection: WhatsApp Flow submit"
  ].join("\n");
}

function buildWhatsAppFlowConfirmationBody(flowData = {}) {
  const branchAr = getFastBookingBranchArabic(flowData.branch || "Dubai");

  return [
    `${BUSINESS_NAME_SPACED} ✨`,
    "",
    "شكراً لك.",
    "تم استلام طلب الحجز.",
    "سيقوم فريقنا بمراجعة التوفر وتأكيد الموعد النهائي قريباً.",
    "",
    `الفرع: ${branchAr}`,
    `الوقت المفضل: ${flowData.preferredTime || "Flexible / Any available time"}`,
    "",
    "------------------------------",
    "",
    `${BUSINESS_NAME_SPACED} ✨`,
    "",
    "Your booking request has been received.",
    "Our team will check availability and confirm the exact appointment shortly.",
    "",
    `Branch: ${flowData.branch || "Dubai"}`,
    `Preferred time: ${flowData.preferredTime || "Flexible / Any available time"}`
  ].join(String.fromCharCode(10));
}


async function handleWhatsAppFlowBookingSubmit({
  from,
  message,
  incomingPhoneNumberId,
  lineConfig,
  profileName
}) {
  if (!isWhatsAppFlowReply(message)) {
    return false;
  }

  const flowData = getWhatsAppFlowBookingData(message, lineConfig);

  if (!flowData) {
    return false;
  }

  const selectedBranch = flowData.branch || lineConfig.branch || "Dubai";
  const requestMessage = buildWhatsAppFlowBookingRequestMessage(flowData);

  addInboxMessage(
    from,
    "customer",
    requestMessage,
    "Booking Request",
    incomingPhoneNumberId,
    {
      customerName: profileName,
      messageType: "WhatsApp Flow Submit"
    }
  );

  setConversationStatus(from, "Booking Request");
  await saveConversationStateToGoogleSheetFromServer({
    phone: from,
    phoneNumberId: incomingPhoneNumberId,
    branch: selectedBranch,
    status: "Booking Request",
    assignee: getBranchTeamAssignee(selectedBranch),
    tags: ["Booking", "WhatsApp Flow", "Need Confirmation"],
    updatedBy: "WhatsApp Flow"
  });

  await saveBookingRequestToGoogleSheetFromServer({
    phone: from,
    phoneNumberId: incomingPhoneNumberId,
    customerName: profileName,
    branch: selectedBranch,
    message: requestMessage,

## SAVE BOOKING REQUEST
2120:  await saveBookingRequestToGoogleSheetFromServer({
2550:    await saveBookingRequestToGoogleSheetFromServer({
3028:async function saveBookingRequestToGoogleSheetFromServer({ phone, phoneNumberId, customerName = "", branch = "", message = "", requestType = "Booking Request", bookingStatus = "Pending" }) {
3046:    action: "saveBookingRequest",
16082:      await saveBookingRequestToGoogleSheetFromServer({

## FLOW HANDLED / BOOKING DATA CALLS
1999:function getWhatsAppFlowBookingData(message, lineConfig = {}) {
2035:function buildWhatsAppFlowBookingRequestMessage(flowData = {}) {
2088:  const flowData = getWhatsAppFlowBookingData(message, lineConfig);
2095:  const requestMessage = buildWhatsAppFlowBookingRequestMessage(flowData);

## STAFF NOTIFICATION USAGE
369:async function sendStaffNotificationTextMessage(to, body, phoneNumberId = DUBAI_PHONE_NUMBER_ID, routing = {}) {
381:  console.log(`[Staff Notify Send] preparing branch=${routing.branch || lineConfig.branch} fromPhoneNumberId=${finalPhoneNumberId} to=${to} env=${routing.envName || "UNKNOWN"} fallback=${Boolean(routing.fallbackUsed)}`);
402:    console.log(`[Staff Notify Send] failed branch=${routing.branch || lineConfig.branch} status=${response.status} env=${routing.envName || "UNKNOWN"}`);
405:    console.log(`[Staff Notify Send] success branch=${routing.branch || lineConfig.branch} status=${response.status} env=${routing.envName || "UNKNOWN"}`);
16558:      console.log(`[Staff Notify Routing] branch=${staffNotificationRouting.branch} phoneNumberId=${staffNotificationRouting.phoneNumberId} env=${staffNotificationRouting.envName} fallback=${staffNotificationRouting.fallbackUsed} hasNumber=${staffNotificationRouting.hasNumber}`);
16562:      console.log(`[Staff Notify Send] skipped branch=${staffNotificationRouting.branch} reason=missing_staff_number env=${staffNotificationRouting.envName}`);
16600:        await sendStaffNotificationTextMessage(staffNotificationNumber, staffBody, incomingPhoneNumberId, staffNotificationRouting);
16602:        console.log(`[Staff Notify Send] failed branch=${staffNotificationRouting.branch} reason=exception env=${staffNotificationRouting.envName}`);

## TEAM INBOX SEND ROUTES
424:  if (sender === "staff") return "Human Reply";
3782:      "Human Reply",
3874:    setConversationStatus(to, "Human Reply");
3875:    addInboxMessage(to, "staff", body, "Human Reply", phoneNumberId, { messageType: "Human Reply" });
3880:      status: "Human Reply",
3883:      updatedBy: "Team Inbox Human Reply"
3925:app.post("/api/send-image", protectInbox, async (req, res) => {
3957:    setConversationStatus(to, "Human Reply");
3968:      "Human Reply",
3976:      status: "Human Reply",
4987:    .send-image-btn {
5000:    .send-image-btn:hover {
6047:    .reply-panel .send-image-btn {
6241:    .reply-panel .composer-block .send-image-btn {
6250:    .reply-panel .composer-block .send-image-btn::before {
6995:    .chat-composer-wrap .send-image-btn {
7407:    .chat-composer-wrap .send-image-btn {
8147:    .chat-composer-wrap .send-image-btn {
8203:      .chat-composer-wrap .send-image-btn { height: 30px !important; min-height: 30px !important; }
8987:    .chat-composer-wrap .send-image-btn,
10516:    .chat-composer-wrap .send-image-btn {
10837:    .chat-composer-wrap .send-image-btn {
10948:      .chat-composer-wrap .send-image-btn {
11940:    .chat-composer-wrap .send-image-btn {
12744:.chat-composer-wrap .send-image-btn {
12760:.chat-composer-wrap .send-image-btn::before {
12766:.chat-composer-wrap .send-image-btn:hover {
12830:  .chat-composer-wrap .send-image-btn {
12908:.chat-composer-wrap .send-image-btn {
12917:.chat-composer-wrap .send-image-btn::before {
12947:  .chat-composer-wrap .send-image-btn {
13062:            <button type="button" class="reference-pill" data-status="Human Reply">Human Reply</button>
13203:                      <button type="button" class="send-image-btn" id="sendImageBtn">▧ Send image</button>
14335:  if (status === "Human Reply") return "staff";
14342:  if (sender === "staff") return "Human Reply";
14368:  // customer = Customer Reply, staff = Human Reply, bot = Bot Reply.
14370:  if (latestMessage.sender === "staff") return "Human Reply";
14386:      s !== "Human Reply" &&
14619:      liveNotificationAudioContext.resume().catch(function() {});
14640:      ctx.resume().catch(function() {});
14748:    "Human Reply",
14993:    // Customer Reply / Human Reply / Bot Reply search the full conversation.
15323:    const res = await fetch("/api/send-image", {

## API ROUTES RELATED TO CONVERSATION STATE
418:const conversationStatus = {};
461:    status: conversationStatus[phone] || status,
592:function setConversationStatus(phone, status) {
593:  conversationStatus[phone] = status;
642:  const memoryStatus = conversationStatus[phone] || "";
651:    conversationStatus[phone] = savedStatus;
2109:  setConversationStatus(from, "Booking Request");
2110:  await saveConversationStateToGoogleSheetFromServer({
2452:      setConversationStatus(from, "WhatsApp Flow - Opened");
2453:      await saveConversationStateToGoogleSheetFromServer({
2487:    setConversationStatus(from, "Fast Booking - Choose Branch");
2488:    await saveConversationStateToGoogleSheetFromServer({
2515:    setConversationStatus(from, "Fast Booking - Choose Time");
2516:    await saveConversationStateToGoogleSheetFromServer({
2539:    setConversationStatus(from, "Booking Request");
2540:    await saveConversationStateToGoogleSheetFromServer({
2978:async function saveConversationStateToGoogleSheetFromServer({ phone, phoneNumberId, branch, status, assignee, tags, updatedBy = "Auto Intent Tags" }) {
2995:    action: "saveConversationState",
3803:app.post("/api/conversation-state", protectInbox, async (req, res) => {
3812:      action: "saveConversationState",
3874:    setConversationStatus(to, "Human Reply");
3876:    await saveConversationStateToGoogleSheetFromServer({
3957:    setConversationStatus(to, "Human Reply");
3972:    await saveConversationStateToGoogleSheetFromServer({
4056:    setConversationStatus(phone, status);
13128:            <select id="conversationStatusSelect" class="conversation-status-select" aria-label="Conversation status">
13323:            <select id="conversationStatusSelect" style="display:none;" aria-hidden="true">
13399:const conversationStatusSelect = document.getElementById("conversationStatusSelect");
13731:async function saveConversationStateToGoogleSheet(c) {
13735:    await fetch("/api/conversation-state", {
15112:    if (conversationStatusSelect) {
15113:      conversationStatusSelect.value = "Open";
15114:      conversationStatusSelect.disabled = true;
15139:  if (conversationStatusSelect) {
15141:    conversationStatusSelect.value = allowedStatuses.includes(c.status) ? c.status : "Open";
15142:    conversationStatusSelect.disabled = false;
15378:        await saveConversationStateToGoogleSheet(currentConversation);
15380:      if (conversationStatusSelect) conversationStatusSelect.value = status;
15502:if (conversationStatusSelect) {
15503:  conversationStatusSelect.addEventListener("change", function() {
15504:    updateStatus(conversationStatusSelect.value || "Open");
15520:      await saveConversationStateToGoogleSheet(currentConversation);
15552:        await saveConversationStateToGoogleSheet(currentConversation);
15692:      setConversationStatus(from, autoIntentWorkflow.status);
15693:      await saveConversationStateToGoogleSheetFromServer({
15705:      setConversationStatus(from, "Opted In");
15744:      setConversationStatus(from, "Opted Out");
15780:      setConversationStatus(from, "Reminder Declined");
15844:      setConversationStatus(from, "Talk to Team");
15845:      await saveConversationStateToGoogleSheetFromServer({
15888:      setConversationStatus(from, "Service Flow - Opened");
15889:      await saveConversationStateToGoogleSheetFromServer({
15949:      setConversationStatus(from, "Consultation Flow - Opened");
15950:      await saveConversationStateToGoogleSheetFromServer({
16012:      setConversationStatus(from, "Booking Menu");
16013:      await saveConversationStateToGoogleSheetFromServer({
16133:      setConversationStatus(from, "Location Requested");
16190:      setConversationStatus(from, "Call Requested");
16258:      setConversationStatus(from, "Media Requested");
16323:      setConversationStatus(from, "Booking Menu");
16338:      setConversationStatus(from, "Service Appointment");
16360:      setConversationStatus(from, "Service Interest");
16389:      setConversationStatus(from, "Service Interest");
16423:      setConversationStatus(from, "Price Question");
16452:      setConversationStatus(from, "Consultation Request");
16488:      setConversationStatus(from, "Talk to Team");
16489:      await saveConversationStateToGoogleSheetFromServer({
16528:      addInboxMessage(from, "bot", formatButtonLog(replyText, replyButtons), conversationStatus[from] || "Bot", incomingPhoneNumberId, { customerName: profileName, messageType: "Bot Reply" });
16531:      addInboxMessage(from, "bot", replyText, conversationStatus[from] || "Bot", incomingPhoneNumberId, { customerName: profileName, messageType: "Bot Reply" });
16543:        conversationStatus[from] || "Bot",
